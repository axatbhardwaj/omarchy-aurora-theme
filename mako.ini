include=~/.local/share/omarchy/default/mako/core.ini

text-color=#d3ccf4
# Violet frame at ~50% so it reads as support, not chrome; green stays the focus cue.
border-color=#9d8cff80
border-radius=14
# 0x8C = 55% alpha, matching waybar, so the notifications layerrule blur renders.
background-color=#0b0c148C
