include=~/.local/share/omarchy/default/mako/core.ini

text-color=#c9eedb
# Green frame at ~55% alpha so it matches the active window border without going solid.
border-color=#62e2a48c
border-radius=14
# 0x8C = 55% alpha, matching waybar, so the notifications layerrule blur renders.
background-color=#0a100d8C
